name: Deploy Web Admin → Vercel

on:
  push:
    branches:
      - main
    paths:
      - 'web-admin/**'
      - '.github/workflows/deploy-web-admin.yml'

jobs:
  deploy:
    name: Deploy Web Admin
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Vercel CLI
        run: npm install -g vercel@latest

      - name: Pull Vercel Environment
        run: vercel pull --yes --environment=production --cwd web-admin --token=${{ secrets.VERCEL_TOKEN }}

      - name: Build Project
        run: vercel build --prod --cwd web-admin --token=${{ secrets.VERCEL_TOKEN }}

      - name: Deploy to Vercel
        timeout-minutes: 8
        run: vercel deploy --prebuilt --prod --cwd web-admin --token=${{ secrets.VERCEL_TOKEN }}
