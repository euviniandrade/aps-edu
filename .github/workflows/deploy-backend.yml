# Backend agora é Google Apps Script — deploy manual via script.google.com
# Este workflow está desativado
name: Backend (Google Apps Script — deploy manual)

on:
  workflow_dispatch:

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: |
          echo "O backend agora roda como Google Apps Script."
          echo "Para atualizar: abra script.google.com, cole o conteúdo de apps-script/Code.js e faça novo deploy."
